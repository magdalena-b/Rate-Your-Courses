import { FilterByEctsPipe } from './filter-by-ects.pipe';

describe('FilterByEctsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByEctsPipe();
    expect(pipe).toBeTruthy();
  });
});
